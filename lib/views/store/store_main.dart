import 'package:flutter/material.dart';

class StoreMain extends StatelessWidget {
  const StoreMain({super.key});
  static const String routeName = '/store-main';
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
